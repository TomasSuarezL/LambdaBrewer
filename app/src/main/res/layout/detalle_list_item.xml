<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">
    <TextView
        android:id="@+id/l_receta_detalle"
        style="@style/RecyclerLabel"
        tools:text="Marca: "
        android:paddingRight="8dp"/>

    <TextView
        android:id="@+id/tv_receta_detalle"
        style="@style/RecyclerTextView"
        tools:text="Carrefour"
        android:paddingRight="8dp"
        android:layout_toRightOf="@id/l_receta_detalle"/>
    <!--This View serves as a visual divider between list items-->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#dadada"
        android:layout_marginLeft="8dp"
        android:layout_marginRight="8dp"
        android:layout_below="@id/l_receta_detalle"/>

</RelativeLayout>